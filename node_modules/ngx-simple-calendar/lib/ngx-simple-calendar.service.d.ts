import { CalendarDay, CalendarEvent } from './data-models/data-calendar';
export declare class NgxSimpleCalendarService {
    constructor();
    getDaysInMonth: (month: number, year: number) => number;
    getFirstDayInMonth: (month: number, year: number) => number;
    getLastDayInMonth: (month: number, year: number) => number;
    setupCalendar(month: number, year: number, events: CalendarEvent[]): CalendarDay[];
    getMonthYearText: (monthNumber: number, year: number) => string;
    private getDaysFromLastMonth;
    private getDaysInCurrentMonth;
    private getDaysFromNextMonth;
    private markCurrentDate;
    private isWeekend;
    private getDateTimeLastOfDay;
    generatedayObject(year: number, month: number, day: number, activeMonth?: boolean): {
        number: number;
        startDateTime: Date;
        endDateTime: Date;
        month: number;
        year: number;
        weekend: boolean;
        today: boolean;
        activeMonth: boolean;
        events: any[];
    };
    private addEvents;
    private handleFullDayEvent;
    private handleEventInterval;
}
